server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mall_ic?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.mall.ic.model
  configuration:
    map-underscore-to-camel-case: true 